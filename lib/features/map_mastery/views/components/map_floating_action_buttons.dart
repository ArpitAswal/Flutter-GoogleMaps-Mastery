import 'package:flutter/material.dart';

// TODO Phase 5: Implement MapFloatingActionButtons
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class MapFloatingActionButtons extends StatelessWidget {
  const MapFloatingActionButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
