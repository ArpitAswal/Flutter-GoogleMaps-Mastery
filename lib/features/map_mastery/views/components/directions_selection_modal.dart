import 'package:flutter/material.dart';

// TODO Phase 5: Implement DirectionsSelectionModal
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class DirectionsSelectionModal extends StatelessWidget {
  const DirectionsSelectionModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
