import 'package:flutter/material.dart';

// TODO Phase 5: Implement SearchAutocompleteOverlay
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class SearchAutocompleteOverlay extends StatelessWidget {
  const SearchAutocompleteOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
