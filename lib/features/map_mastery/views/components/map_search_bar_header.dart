import 'package:flutter/material.dart';

// TODO Phase 5: Implement MapSearchBarHeader
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class MapSearchBarHeader extends StatelessWidget {
  const MapSearchBarHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
