import 'package:flutter/material.dart';

// TODO Phase 5: Implement RouteMetricsBottomSheet
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class RouteMetricsBottomSheet extends StatelessWidget {
  const RouteMetricsBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
