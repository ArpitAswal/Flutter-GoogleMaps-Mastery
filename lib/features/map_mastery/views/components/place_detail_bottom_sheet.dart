import 'package:flutter/material.dart';

// TODO Phase 5: Implement PlaceDetailBottomSheet
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class PlaceDetailBottomSheet extends StatelessWidget {
  const PlaceDetailBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
