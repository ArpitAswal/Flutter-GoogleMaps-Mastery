import 'package:flutter/material.dart';

// TODO Phase 6: Implement RemoteViewerScreen
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class RemoteViewerScreen extends StatelessWidget {
  const RemoteViewerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
