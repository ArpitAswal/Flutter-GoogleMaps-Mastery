import 'package:flutter/material.dart';

// TODO Phase 6: Implement SessionShareDialog
// No setState — all state is emitted by Cubits and consumed via BlocBuilder/BlocListener.
class SessionShareDialog extends StatelessWidget {
  const SessionShareDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // placeholder
  }
}
